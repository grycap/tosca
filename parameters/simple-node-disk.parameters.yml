inputs:
    num_cpus:
      tab: VM Data

    mem_size:
      tab: VM Data

    num_instances:
      tab: VM Data

    storage_size:
      tab: VM Data

    mount_path:
      tab: VM Data

    ports:
      tab: VM Data

    num_gpus:
      tab: GPU Data

    gpu_vendor:
      tab: GPU Data

    gpu_model:
      tab: GPU Data
tabs:
  - VM Data
  - GPU Data


