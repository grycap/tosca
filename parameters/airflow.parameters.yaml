inputs:
    airflow_admin_password:
      tab: Airflow Data

    airflow_webserver_secret_key:
      tab: Airflow Data

    airflow_dags_repo:
      tab: Airflow Data

    airflow_dags_repo_subpath:
      tab: Airflow Data

    airflow_dags_branch:
      tab: Airflow Data

tabs:
  - Airflow Data
