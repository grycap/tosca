inputs:
    num_cpus:
      tab: VMs features
    mem_size:
      tab: VMs features
    num_instances:
      tab: VMs features

    proxy_host:
      tab: Proxy Host Info
    private_key:
      tab: Proxy Host Info
    user:
      tab: Proxy Host Info
    private_key:
      tag_type: textarea
      tab: Proxy Host Info

tabs:
  - VMs features
  - Proxy Host Info