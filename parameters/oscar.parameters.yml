inputs:
    wn_num:
      tab: HW Data

    fe_cpus:
      tab: HW Data

    fe_mem:
      tab: HW Data

    fe_instance_type:
      tab: HW Data

    wn_cpus:
      tab: HW Data

    wn_mem:
      tab: HW Data

    wn_instance_type:
      tab: HW Data

    storage_size:
      tab: HW Data

    admin_token:
      tab: OSCAR parameters

    oscar_password:
      tab: OSCAR parameters

    minio_password:
      tab: OSCAR parameters

    cert_user_email:
      tab: OSCAR parameters

    user_id:
      tab: OSCAR parameters
      tag_type: userid

    selected_vo:
      tab: OSCAR parameters
      tag_type: vo_list

    cluster_name:
      tab: OSCAR parameters
      tag_type: random

    domain_name:
      tab: OSCAR parameters
      tag_type: hidden

    kube_nvidia_support:
      tab: OSCAR parameters

    kube_install_yunikorn:
      tab: OSCAR parameters

tabs:
  - HW Data
  - OSCAR parameters
