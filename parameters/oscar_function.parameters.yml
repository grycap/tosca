inputs:

    function_name:
      tab: OSCAR function

    function_memory:
      tab: OSCAR function
      pattern: ^([+-]?[0-9.]+)([eEinumkKMGTP]*[-+]?[0-9]*)$
      tag_type: text

    function_cpu:
      tab: OSCAR function

    function_image:
      tab: OSCAR function

    function_script:
      tab: OSCAR function
      tag_type: textarea

    function_input:
      tab: OSCAR function

    function_output:
      tab: OSCAR function

tabs:
  - OSCAR function