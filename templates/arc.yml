tosca_definitions_version: tosca_simple_yaml_1_0

imports:
  - ec3_custom_types: https://raw.githubusercontent.com/grycap/tosca/arc/custom_types.yaml

description: >
  TOSCA for configuring an ARC endpoint

metadata:
  template_version: "1.0.0"
  template_name: ARC
  display_name: Configure an ARC endpoint
  icon: images/pulsar.png
#  tabs:
#    Pulsar configuration: message_queue_.*
  parents:
    - slurm_cluster.yml

topology_template:
#  inputs:
#
#    message_queue_user:
#      type: string
#      description: User for the message queue
#      default: changeme
#    message_queue_password:
#      type: string
#      description: Password for the message queue
#      default: changeme

  node_templates:

    arc_node:
      type: tosca.nodes.indigo.ARC
#      properties:
#        mq_user: { get_input: message_queue_user }
#        mq_password: { get_input: message_queue_password }
      requirements:
        - lrms: lrms_front_end
        - host: lrms_server
