tosca_definitions_version: tosca_simple_yaml_1_0

imports:
  - grycap_custom_types: https://raw.githubusercontent.com/grycap/tosca/main/custom_types.yaml

metadata:
  template_name: ENES Data Space service
  template_version: "1.0.0"
  display_name: ENES Data Space service on top of Kubernetes
  icon: images/eds.png
  link: 
    parent: kubernetes.yaml
    childs:
      - enes.yaml

description: TOSCA template for launching ENES Data Space service on top of a Kubernetes Cluster.

topology_template: {}
