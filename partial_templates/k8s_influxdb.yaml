tosca_definitions_version: tosca_simple_yaml_1_0

imports:
  - grycap_custom_types: https://raw.githubusercontent.com/grycap/tosca/main/custom_types.yaml

description: Deploy InfluxDB on top of a Kubernetes Virtual Cluster.

metadata:
  version: "1.0.0"
  display_name: Deploy InfluxDB on top of a Kubernetes Virtual Cluster
  icon: images/k8s_influxdb.png
  link: 
    parent: kubernetes.yaml
    childs:
      - influxdb.yaml

topology_template: {}